**Learning:** Root level logic blocks migration into monorepos due to lack of encapsulation.
**Action:** Unify all standalone project assets into a single load-bearing directory before migrating to a monorepo.
